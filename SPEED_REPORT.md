# 100 部候选与片库扩充验收

日期：2026-09-21。片库 26,252 部，策略 v3-100。

## 实际速度

同一片库、相同代码及修复后的完整字段，三条固定需求各做一次 24/100 对照，轮换执行先后，共六次真实无缓存推荐。实际模型 jev-1.13.0。耗时包含本机到 Jev 的网络、意图判断、全库召回和评分；不含首次片库加载、浏览器资源下载或 Cloudflare 网络。

| 需求 | 24 部：8 部/批，并发3 | 100 部：20 部/批，并发5 |
|---|---:|---:|
| 想看一部日本电影，关于家庭和日常 | 3.23 s | 2.65 s |
| 孤独但不悲伤，想看人与人相遇的故事 | 1.49 s | 1.25 s |
| 像《盗梦空间》一样，让我脑子转起来，两小时以内 | 1.43 s | 1.19 s |

本次样本没有出现候选扩张后明显变慢。请求并行、连接预热和上游服务波动都会影响结果；不能据此声称 100 部一定比 24 部更快，也不能外推高并发性能。

每次总模型请求从4次变为6次（均含1次意图判断）。三条需求合计输入 token 从42,038增至152,334，约3.62倍；输出 token 从2,292增至6,950。这里记录实际用量，不推测账户计价。缓存命中不调用 Jev。

100 部版本的《盗梦空间》相似查询补入《红辣椒》《睁开你的双眼》《感官游戏》等，返回结果均有已知片长且不超过120分钟。心情查询仍可能出现不够贴合“不要悲伤”的电影；扩大候选数没有解决全部语义限制。

## 召回与数据验证

- 同一份新片库、18条固定人工评估需求：相关条目进入候选共36→55，参考相关条目共104。不是独立盲测，也不代表用户满意率。历史24部回归测试仍保留。
- 原有22,296个ID与内容逐项保留，新增3,956部；总计26,252唯一ID，73种语言代码。新增全部满足TMDB≥7分、≥30票、发行不晚于2025、有海报与简介，27条不合格记录排除。已有318部榜单电影及来源保留。
- 修复了生产片库遗漏originalTitle、language、rating、votes的问题，并加入本地完整片库与生产投影的召回一致性测试。
- 19项测试通过，生产构建通过。100部均被评分、最后一批不遗漏、并发最多5、失败不重试及不返回部分推荐均有检查。

完整逐次请求时长、token和返回电影见 `data/eval/benchmark-100.json`；扩库依据见 `data/curation/expansion-report.json`。
