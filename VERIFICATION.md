# 验收记录

日期：2026-09-20

## 已完成

- Node 自动化测试覆盖硬片长限制（未知时排除）、中文年代、跨语言召回、缺失/异常 Jev 响应、空过滤对象校验、上游错误不得伪造结果和片库 ID 唯一性。
- Vite 生产构建成功；GitHub Pages 项目子路径资源可正常加载。
- Cloudflare Worker 的 Jev 实际调用成功，实际模型 `jev-1.13.0`。Wiki 启动库的梦境科幻查询耗时 1484 ms，返回《盗梦空间》，input_tokens 9671 / output_tokens 834。速度是单次观察，不是 SLA。
- 在 GitHub Pages 实际页面完成访问码填写与中文查询，浏览器可跨域调用 Worker，显示真实结果。
- 在正式页面查询“90 年代的犯罪片，氛围越浓越好”，结果均在 1990—1999 年，包含《七宗罪》。
- 在本地浏览器验证电影详情、添加收藏、片单视图、刷新后保留收藏。
- 在 390px 宽 iframe 真实渲染手机布局，document clientWidth 与 scrollWidth 均为 390，无水平溢出，海报扇形正确裁切、卡片双列。
- Worker 本地模拟返回：无访问码 401，不受信任 Origin 403。
- 扫描前端产物与仓库应用源码，没有包含 Jev API 密钥。

## 证据边界

Jev 的数次中文样例测试不能证明广泛推荐准确率。候选召回仍为词表/主题启发式检索，24 部候选可能漏掉更合适的电影。不是逐部观影或百万级检索测试。取消前端请求后，已发出的服务端推理可能继续完成。

## 正式 TMDB 片库

成功导入 5000 部电影，覆盖 21 种原始语言；4669 部有中文简介，4997 部有片长。补齐了首页精选电影。导入失败计数为 0；原始 API 响应缓存在本机，凭据不进入仓库。

### 正式片库真实 Jev 样例

- “想看一部温暖的动画电影，90分钟以内”：返回《小小北极熊》（78 分钟）、《星际宝贝》（85 分钟），本地端到端 3362 ms。
- “像《盗梦空间》一样，让我脑子转起来”：返回包含《红辣椒》《童梦失魂夜》的 6 部电影，1774 ms。参考电影简介与候选一起提供给 Jev。
- “关于孤独、城市和人与人相遇的故事”：返回包含《她》《东京！》的 5 部电影，1076 ms。
- 浏览器电影详情实际显示《布达佩斯大饭店》中文简介、100 分钟、TMDB 8.0 和来源链接；片长下拉选 90 分钟后显示 729 部候选。

这些结果用于确认端到端能力与硬条件正确，推荐是否合口味仍需使用者判断。

## 发布缓存修复

浏览器曾缓存旧版 Wiki 片库，导致 TMDB 结果 ID 无法映射。已将片库 SHA-256 摘要写入构建产物和数据请求 URL；同版本可缓存，更新数据自动换 URL。接口返回不存在的电影 ID 时显示明确的刷新提示，不再显示虚假的空结果。

## 全球片库扩容至 20,000 部（2026-09-20）

- 原有 5,000 个 TMDB ID 全部保留，新增 15,000 部；总计 20,000 个唯一 ID，覆盖 46 种原始语言，导入失败数为 0。逐项检查海报、非空简介和来源链接。
- 浏览器卡片索引未压缩 6.72 MiB，gzip 约 1.45 MiB；简介与演员按点击读取 40 个详情分片之一。索引与详情的 20,000 个 ID 完整对应。
- Worker 从 20 个静态资源分片读取 Jev 所需资料，每批最多并行读取 4 片；代码包 gzip 6.08 KiB，部署启动耗时 2 ms。线上版本：666d5c07-5757-43cb-97a7-2e437a00579a。
- 11 项 Node 测试及生产构建通过。新增测试覆盖分片合并、并发复用、版本错误后的恢复及重复 ID 拒绝；并行读取改动后相关 3 项测试再次通过。
- 线上 health 显示 catalogCount: 20000。Jev 查询“想看《滑索惊魂》，高空冒险与求生的惊悚电影”返回新增 ID tmdb-1482547；浏览器实际显示推荐卡片和完整中文简介、95 分钟片长。
- 并行读取上线后，实际查询“想看一部温暖的动画电影，90分钟以内”返回 10 部，包括《木偶奇遇记》《老鼠也能上天堂》，全部有已知片长且不超过 90 分钟。端到端观察 3975 ms，其中 Jev 流程 1327 ms；实际模型 jev-1.13.0。这是单次观察，不是稳定延迟保证。
- GitHub Pages 工作流 35514237224 成功，线上页面已实测显示 20,000 部电影。仍然由 Jev 选片，没有地区限定。

## 影史佳作与高口碑电影扩充（2026-09-21）

- 总数 22,296，唯一 ID 22,296；保留此前全部 20,000 部，新增 2,296 部，覆盖 58 种原始语言。新增中 54 部来自核实的影史榜单，其余均通过 TMDB 评分、评价人数及资料完整度门槛。
- BFI Sight and Sound 2022 榜单覆盖 262/263（含并列名次）；缺项《电影史》在 TMDB 属于电视剧，未用单独章节冒充整部。AFI 2007 百年百大覆盖 100/100。两榜合计对应 318 个电影 ID。
- 12 项自动化测试通过，生产构建和 Wrangler dry-run 通过。新增测试验证明确经典偏好的检索加权，普通查询及排除经典的查询不受该加权影响。
- Worker 正式版本 525861b4-8654-48df-882c-218ff2d5c9ee；catalogVersion cebabfc710102ef7，health 实测 catalogCount 22296。GitHub Pages 工作流 35537809175 成功，对应提交 adcc0e4。
- 正式浏览器显示 22,296 部。查询“想看《绿光》，关于孤独、假期里寻找陪伴的影史佳作”，Jev 从 24 部候选中返回新增电影《绿光》，界面报告 1.7s。电影详情完整加载中文简介、99 分钟、演员及可点击的 BFI 榜单来源。单次测试证明新片可实际被推荐及打开，不代表全面推荐准确率。
